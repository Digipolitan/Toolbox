{
  "missing_fields": {
    "en": "These fields are missing : ${0}"
  },
  "invalid_objectIds": {
    "en": "These fields must be ObjectIds : ${0}"
  },
  "invalid_arrays": {
    "en": "These fields must be Arrays (minLength:${0}) (maxLength:${1}) : ${2}"
  },
  "invalid_strings": {
    "en": "These fields must be Strings (minLength:${0}) (maxLength:${1}) : ${2}"
  },
  "invalid_numbers": {
    "en": "These fields must be Numbers (min:${0}) (max:${1}) : ${2}"
  },
  "invalid_phone_numbers": {
    "en": "These fields must be phone numbers : ${0}"
  },
  "invalid_emails": {
    "en": "These fields must be emails : ${0}"
  },
  "invalid_slugs": {
    "en": "These fields must be slugs : ${0}"
  }
}
